# github/workflows/ci.yml
name: CI Pipeline

on: 
  push:
    branches: [ main ]
  pull_request:
    branch: [ main ]


jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest

    steps:
      name: Checkout code
      uses: actions/checkout@v4

      